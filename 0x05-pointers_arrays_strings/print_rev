#include "main.h"

/**
* print_rev - Prints a string in reverse, followed by a line
*
* @s: parameter
*
* Return: void
*/

void print_rev(char *s)
{
	int i;

	if !(i > 0)
		i = 0;

	while (s[i] != '\0')
	{
		i++;
		print_rev(char s);
	}

	_putchar(s[i]);
}
